import { useEffect, useState } from 'react'

const tabs = ['Home', 'Shop', 'Scan', 'Rewards', 'Profile']
const products = [
  { name: 'Cloud Dew Serum', brand: 'Botanica Labs', price: 34, stock: 'Low stock', color: 'peach', icon: 'drop' },
  { name: 'Rose Renewal Cream', brand: 'Moss & Moon', price: 42, stock: 'Running low', color: 'rose', icon: 'flower' },
  { name: 'Sunlit Mineral SPF 50', brand: 'Dune & Dew', price: 29, stock: 'On track', color: 'gold', icon: 'sun' },
  { name: 'Velvet Fig Lip Oil', brand: 'Kindred Color', price: 24, stock: 'In stock', color: 'plum', icon: 'lips' },
]
const iconPaths = { home: <><path d="m3 10 9-7 9 7M5 9v11h14V9M9 20v-6h6v6" /></>, bag: <><path d="M5 8h14l1 12H4L5 8Z" /><path d="M9 8a3 3 0 0 1 6 0" /></>, scan: <><path d="M4 8V5h3M17 4h3v4M20 16v3h-3M7 20H4v-4M7 12h10M7 15h6" /></>, award: <><circle cx="12" cy="8" r="5" /><path d="m8.5 12.5-1 8 4.5-2 4.5 2-1-8" /></>, user: <><circle cx="12" cy="8" r="4" /><path d="M4 21a8 8 0 0 1 16 0" /></>, flower: <><circle cx="12" cy="12" r="2.5" /><path d="M12 9c-4-7 4-7 0 0ZM15 12c7-4 7 4 0 0ZM12 15c4 7-4 7 0 0ZM9 12c-7 4-7-4 0 0Z" /></>, leaf: <><path d="M20 4C10 4 5 8 5 15c0 3 2 5 5 5 7 0 10-6 10-16Z" /><path d="M4 20c3-5 7-8 12-10" /></>, check: <path d="m5 12 4 4L19 6" />, plus: <><path d="M12 5v14M5 12h14" /></> }
function Icon({ name, size = 20 }) {
  const headerIcon = name === 'search' ? <><circle cx="11" cy="11" r="6.5" /><path d="m16 16 4 4" /></> : name === 'bell' ? <><path d="M18 9a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9" /><path d="M10 22h4" /></> : name === 'drop' ? <path d="M12 3s6 6 6 11a6 6 0 0 1-12 0c0-5 6-11 6-11Z" /> : name === 'sun' ? <><circle cx="12" cy="12" r="4" /><path d="M12 2v2M12 20v2M2 12h2M20 12h2M5 5l2 2M17 17l2 2M19 5l-2 2M7 17l-2 2" /></> : name === 'lips' ? <path d="M3 13c3-1 5-5 9-5s6 4 9 5c-3 5-15 5-18 0Z" /> : iconPaths[name]
  return <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">{headerIcon}</svg>
}
function ProductCard({ product, onClick }) { return <button className="mini-card product-card" onClick={onClick}><div className={`product-mark ${product.color}`}><Icon name={product.icon} size={24} /></div><div className="product-copy"><div className="title-row"><strong>{product.name}</strong><span>12 days left</span></div><p>{product.brand} · Maya</p><div className="chip-row"><span className="chip subtle">Leaping Bunny Certified</span><span className="chip accent">{product.stock}</span></div></div></button> }
function Garden({ full = false }) { return <section className="garden card"><div className="garden-header"><div><p className="eyebrow">Reward garden</p><h3>{full ? 'Flowers earned through care' : 'A flower for every good choice'}</h3></div><div className="flower-badge"><Icon name="flower" size={22} /></div></div>{full ? <><p className="garden-explanation">Every verified purchase, reminder, and conscious choice helps your garden grow. Each flower represents a habit you have completed.</p><div className="garden-visual"><div className="flower-stem stem-a"><i /></div><div className="flower-stem stem-b"><i /></div><div className="flower-stem stem-c"><i /></div><div className="flower-stem stem-d"><i /></div></div><div className="garden-legend"><span>2 routines completed</span><span>2 habits in progress</span></div></> : <div className="garden-progress"><div className="progress-bar"><span style={{ width: '68%' }} /></div><div className="progress-meta"><strong>68% grown</strong><span>1,280 points earned</span></div></div>}</section> }
function Home({ go, select }) { return <main className="content"><section className="hero card"><span className="hero-tag">Household essentials</span><h2>4 routines need attention</h2><p>Keep your conscious beauty picks stocked with smart reminders and verified matches.</p><button className="primary-btn" onClick={() => go('Scan')}><Icon name="scan" size={16} /> Scan product</button></section><section className="low-stock"><div className="section-head"><h3>Running low</h3><button className="link-btn" onClick={() => go('Shop')}>View all</button></div><div className="product-list">{products.filter((p) => p.stock !== 'On track').map((p) => <ProductCard key={p.name} product={p} onClick={() => select(p)} />)}</div></section><section className="reminders card soft"><div className="section-head compact"><h3>Upcoming reminders</h3><button className="link-btn">Manage</button></div><div className="reminder-item"><div className="dot" /><div><strong>Mascara refill</strong><p>Maya</p></div><span>Tomorrow</span></div><div className="reminder-item"><div className="dot" /><div><strong>Face mist</strong><p>Chris</p></div><span>Wed</span></div></section><div className="offers-row">{['Partner deal', 'Reward unlock', 'Brand drop'].map((item) => <div className="offer-card card" key={item}><p>{item}</p><strong>{item === 'Partner deal' ? 'Wildflower Trio' : item === 'Reward unlock' ? 'Garden perk' : 'Early access'}</strong><span>{item === 'Partner deal' ? 'Save 15%' : item === 'Reward unlock' ? '+180 points' : 'New serum'}</span><small>Available now</small></div>)}</div><Garden /></main> }
function Shop({ select, go }) { const [query, setQuery] = useState(''); const found = products.filter((p) => p.name.toLowerCase().includes(query.toLowerCase())); return <main className="content page-content"><div className="page-heading"><h2>Shop conscious beauty</h2></div><div className="search-box"><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search products or brands" /></div><button className="scan-banner" onClick={() => go('Scan')}><span className="scan-icon"><Icon name="scan" size={22} /></span><span><strong>Scan a barcode</strong><small>Compare prices and verify certification</small></span>→</button><div className="shop-grid">{found.map((p) => <ProductCard key={p.name} product={p} onClick={() => select(p)} />)}</div></main> }
function Scan() { const [scanned, setScanned] = useState(false); return <main className="content page-content"><div className="page-heading"><h2>Scan to know more</h2></div><section className="scanner card"><div className="scanner-frame"><div className="scan-corner top-left" /><div className="scan-corner top-right" /><div className="scan-line" /><div className="scan-corner bottom-left" /><div className="scan-corner bottom-right" /><Icon name="scan" size={38} /></div><h3>{scanned ? 'Product found' : 'Point your camera at a barcode'}</h3><p>{scanned ? 'We found a verified match and compared current retailer prices.' : 'Hold the barcode inside the frame. The moving line shows when scanning is active.'}</p><div className="scan-instructions"><strong>What this scan checks</strong><span>Certification eligibility</span><span>Ingredient highlights</span><span>Lowest and best-value prices</span></div><button className="primary-btn" onClick={() => setScanned(true)}>{scanned ? 'Scan another product' : 'Start scanning'}</button></section></main> }
function Rewards() { const [claimed, setClaimed] = useState(false); const [showTiers, setShowTiers] = useState(false); const tiers = ['Seedling', 'Meadow Keeper', 'Garden Steward', 'Wildflower Circle']; const tasks = ['Verify one cruelty-free product', 'Add it to a household routine', 'Set a replenishment reminder', 'Complete one partner purchase']; return <main className="content page-content rewards-redesigned"><div className="page-heading"><h2>Your conscious impact</h2><span className="tier-badge"><Icon name="award" size={15} /> Seedling</span></div><section className="earnings-card card"><div><p className="eyebrow">Available earnings</p><strong>1,280 <small>points</small></strong><p>Earned from 12 verified purchases</p></div><div className="earnings-orbit"><Icon name="flower" size={26} /><span>+180</span></div></section><section className="redeem-card card"><div className="section-head compact"><h3>Redeem your earnings</h3><span className="chip subtle">1,280 available</span></div><div className="redeem-options"><button><Icon name="bag" size={18} /><span><strong>$12 beauty credit</strong><small>Use at partner retailers</small></span><b>1,200 pts</b></button><button><Icon name="flower" size={18} /><span><strong>Plant a new flower</strong><small>Grow your reward garden</small></span><b>300 pts</b></button><button><Icon name="leaf" size={18} /><span><strong>Donate to an animal fund</strong><small>Support certified care</small></span><b>500 pts</b></button></div></section><section className="tier-card card"><div className="section-head compact"><h3>Your tier journey</h3><span>1,280 pts</span></div><div className="tier-ladder">{tiers.map((tier, i) => <div className={i === 0 ? 'tier-step current' : 'tier-step'} key={tier}><span className="tier-marker">{i === 0 ? <Icon name="check" size={13} /> : i + 1}</span><div><strong>{tier}</strong><small>{['0–499', '500–1,499', '1,500–2,999', '3,000+'][i]} points</small></div>{i === 0 && <em>Current</em>}</div>)}</div><div className="tier-perks"><span>★ Free shipping</span><span>★ Partner previews</span><span>★ Double points Fridays</span></div><button className="tier-sheet-btn" onClick={() => setShowTiers(true)}>View all tiers</button><small className="reset-note">Points and status reset every year on December 31.</small></section><Garden full /><section className="challenge-card card"><div className="section-head compact"><div><p className="eyebrow">This week's challenge</p><h3>Complete your routine</h3></div><strong>2 of 4</strong></div><div className="challenge-progress"><span /></div><div className="challenge-steps">{tasks.map((task, i) => <div className={i < 2 ? 'challenge-step done' : 'challenge-step'} key={task}><span>{i < 2 && <Icon name="check" size={13} />}</span><p>{task}</p></div>)}</div><button className="secondary-btn" onClick={() => setClaimed(true)}><Icon name={claimed ? 'check' : 'plus'} size={15} /> {claimed ? 'Challenge claimed' : 'Claim +60 after completion'}</button></section>{showTiers && <div className="sheet-backdrop" onClick={() => setShowTiers(false)}><section className="tier-sheet" onClick={(event) => event.stopPropagation()}><div className="sheet-handle" /><div className="sheet-heading"><h3>All tiers</h3><button onClick={() => setShowTiers(false)} aria-label="Close tiers">×</button></div><p className="reset-note">Points and status reset every year on December 31.</p>{tiers.map((tier, i) => <div className={i === 0 ? 'sheet-tier current' : 'sheet-tier'} key={tier}><div><strong>{tier}</strong><small>{['0–499', '500–1,499', '1,500–2,999', '3,000+'][i]} points</small></div><span>{i === 0 ? 'Current' : i === 1 ? 'Early access' : i === 2 ? 'Free shipping' : 'All perks'}</span></div>)}</section></div>}</main> }
function Profile() { const [one, setOne] = useState(true); const [two, setTwo] = useState(true); return <main className="content page-content"><section className="profile-card card"><div className="avatar">CP</div><div><h3>Christina's household</h3><p>3 members · 14 conscious products</p></div><button className="link-btn">Edit</button></section><section className="household-list card"><div className="section-head compact"><h3>Members</h3><button className="link-btn">Add member</button></div><div className="household-row"><span className="member-avatar">C</span><strong>Christina · You</strong></div><div className="household-row"><span className="member-avatar avatar-1">M</span><strong>Maya · 6 products</strong></div><div className="household-row"><span className="member-avatar avatar-2">C</span><strong>Chris · 4 products</strong></div></section><section className="settings-list card"><h3 className="settings-title">Alerts</h3><div className="setting-row"><div><strong>Replenishment alerts</strong><p>Get notified before essentials run out</p></div><button className={one ? 'toggle on' : 'toggle'} onClick={() => setOne(!one)}><span /></button></div><div className="setting-row"><div><strong>Price drop alerts</strong><p>Watch your saved products</p></div><button className={two ? 'toggle on' : 'toggle'} onClick={() => setTwo(!two)}><span /></button></div></section></main> }
function App() { const [tab, setTab] = useState('Home'); const [selected, setSelected] = useState(null); const [toast, setToast] = useState(''); useEffect(() => { const openRetailer = (event) => { if (event.target.matches('.product-sheet .retailer-row:last-child > span')) window.open('https://www.leapingbunny.org/', '_blank', 'noopener,noreferrer') }; document.addEventListener('click', openRetailer); return () => document.removeEventListener('click', openRetailer) }, []); const go = (next) => { setTab(next); setSelected(null) }; const page = tab === 'Home' ? <Home go={go} select={setSelected} /> : tab === 'Shop' ? <Shop go={go} select={setSelected} /> : tab === 'Scan' ? <Scan /> : tab === 'Rewards' ? <Rewards /> : <Profile />; return <div className="app-shell"><div className="phone-frame"><header className="topbar"><div>{tab === 'Home' && <p className="eyebrow">Good afternoon</p>}<h1>{tab === 'Home' ? 'Christina' : tab}</h1></div><div className="icon-group"><button onClick={() => go('Shop')} aria-label="Search"><Icon name="search" /></button><button onClick={() => setToast('You are all caught up')} aria-label="Notifications"><Icon name="bell" /></button></div></header>{page}{selected && <ProductDetail product={{ ...selected, cert: 'Leaping Bunny Certified', lowestPrice: 31, bestValue: selected.price, lastUpdated: 'Today', badge: 'Verified', usage: '12 days left', member: 'Maya', summary: 'A verified conscious beauty choice.', ingredients: ['Plant extracts', 'Vitamin E'], retail: [{ store: 'Earthly', price: 31, note: 'Best value' }] }} onClose={() => setSelected(null)} onReminder={() => setToast('Reminder set')} />}{tab === 'Home' && <div className="reward-strip card"><div><p>Rewards</p><strong>1,280 pts</strong></div><div className="goal-pill">220 to next tier</div></div>}<nav className="bottom-nav">{tabs.map((item) => <button key={item} className={tab === item ? 'nav-item active' : 'nav-item'} onClick={() => go(item)}><span><Icon name={item === 'Home' ? 'home' : item === 'Shop' ? 'bag' : item === 'Scan' ? 'scan' : item === 'Rewards' ? 'award' : 'user'} size={19} /></span><small>{item}</small></button>)}</nav><button className="floating-assistant" onClick={() => setToast('Bunny concierge is ready')}><Icon name="leaf" size={16} /> Ask Bunny</button>{toast && <div className="toast" onClick={() => setToast('')}><Icon name="check" size={16} /> {toast}</div>}</div></div> }

function ProductDetail({ product, onClose, onReminder }) { return <div className="product-sheet-backdrop" onClick={onClose}><aside className="product-sheet" onClick={(event) => event.stopPropagation()}><div className="sheet-handle" /><div className="sheet-heading"><div><p className="eyebrow">Product details</p><h3>{product.name}</h3></div><button onClick={onClose} aria-label="Close product details">×</button></div><div className="detail-product-top"><div className={`product-mark ${product.color}`}><Icon name={product.icon} size={28} /></div><div><strong>{product.brand}</strong><p>Verified conscious beauty match</p></div></div><div className="meta-badges"><span className="chip accent">{product.badge}</span><span className="chip subtle">{product.cert}</span></div><p className="product-summary">{product.summary}</p><div className="pricing-box"><div><span>Lowest price</span><strong>${product.lowestPrice}</strong></div><div><span>Best value</span><strong>${product.bestValue}</strong></div><div><span>Updated</span><strong>{product.lastUpdated}</strong></div></div><div className="ingredients-block"><h4>Ingredient highlights</h4><div className="ingredient-tags">{product.ingredients.map((item) => <span key={item}>{item}</span>)}</div></div><div className="retail-list">{product.retail.map((item) => <div className="retailer-row" key={item.store}><div><strong>{item.store}</strong><small>{item.note}</small></div><span>${item.price}</span></div>)}</div><div className="sheet-actions"><button className="primary-btn block" onClick={onReminder}>Add to profile</button><a className="external-purchase-btn" href="https://www.leapingbunny.org/" target="_blank" rel="noreferrer">Purchase externally</a></div></aside></div> }

export default App
